#include <stdio.h>
#include <stdlib.h>



int length_calc(int input[39]) {
    int endloop = 0;                        //Can set to 1 to end the loop, 0 makes the loop continue

    while(endloop == 0)
    {
        if()
    }


}
